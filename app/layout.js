import './globals.css';

export const metadata = {
  title: 'HerbalBridge | Botanical Wellness Private Label',
  description: 'Custom mugwort foot soak and herbal sachets for wellness brands, retailers and importers.'
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
